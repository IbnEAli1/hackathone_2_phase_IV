apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: webapp
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying!
  DATABASE_URL: "postgresql://user:password@db-host:5432/webapp"
  JWT_SECRET: "CHANGE-ME-replace-with-a-real-secret"
  API_KEY: "CHANGE-ME-replace-with-a-real-api-key"
